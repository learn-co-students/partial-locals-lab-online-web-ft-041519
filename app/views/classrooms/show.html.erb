Classroom Info
<%= render partial: "classroom", locals: {classroom: @classroom} %>

We now want to call out the oldest student: 
<%= render partial: "students/student", locals: {student: @classroom.oldest_student} %>

<%=@classroom.students.each do |x|%>
    <%= render partial: "students/student", locals: {student: x} %>
<% end %>